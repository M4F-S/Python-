# Exercise 7 — ft_seed_inventory
# Authorized: print(), string methods
#
# REQUIRED signature:
#   def ft_seed_inventory(seed_type: str, quantity: int, unit: str) -> None:
#
# Behaviour:
#   seed_type is capitalized (Tomato, Carrot, Lettuce, ...)
#   unit == "packets" -> "<Seed> seeds: <qty> packets available"
#   unit == "grams"   -> "<Seed> seeds: <qty> grams total"
#   unit == "area"    -> "<Seed> seeds: covers <qty> square meters"
#   anything else     -> "Unknown unit type"


def ft_seed_inventory(seed_type: str, quantity: int, unit: str) -> None:
    pass
