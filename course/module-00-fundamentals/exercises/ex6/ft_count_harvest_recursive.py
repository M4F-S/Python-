# Exercise 6 (recursive half) — ft_count_harvest_recursive
# Authorized: input(), int(), print(), range(), helper functions for recursion
#
# Same output as the iterative version, but the counting must be done
# recursively (no `while`, no `for ... in range()` to do the counting).
# You may use: a nested helper, default parameter values, or a separate
# helper function.


def ft_count_harvest_recursive():
    pass
