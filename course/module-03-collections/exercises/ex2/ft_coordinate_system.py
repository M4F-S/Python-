# Exercise 2 — ft_coordinate_system
# Authorized: import math, math.sqrt(), input(), round(), print()
#
# get_player_pos() asks repeatedly until valid "x,y,z" given and returns
# a tuple of three floats.
# Then print coordinates, distance to origin, ask again, print distance
# between the two.

import math


def get_player_pos() -> tuple[float, float, float]:
    pass


if __name__ == "__main__":
    pass
