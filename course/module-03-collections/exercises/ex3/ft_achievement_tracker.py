# Exercise 3 — ft_achievement_tracker
# Authorized: len(), print(), import random, random.*, set(), set.union(),
#             set.intersection(), set.difference()
#
# gen_player_achievements(pool) returns a randomly sized subset (set).
# Build a dict of {player_name: set} for at least 4 players.
# Print each player's set, all distinct, common, exclusive, and missing.

import random


ACHIEVEMENTS: list[str] = [
    "Boss Slayer", "Speed Runner", "Collector Supreme",
    "Master Explorer", "Strategist", "Crafting Genius",
    "Untouchable", "Unstoppable", "Sharp Mind",
    "Treasure Hunter", "World Savior", "First Steps",
    "Survivor", "Hidden Path Finder",
]


def gen_player_achievements(pool: list[str]) -> set[str]:
    pass


if __name__ == "__main__":
    pass
