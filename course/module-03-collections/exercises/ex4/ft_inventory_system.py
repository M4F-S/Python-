# Exercise 4 — ft_inventory_system
# Authorized: import sys, sys.argv, len(), print(), sum(), list(),
#             round(), dict.keys(), dict.values(), dict.update()
#
# Parse "item:qty" pairs from argv. Discard invalid tokens (missing ':',
# non-int qty, duplicate name) with error messages.
# Then print inventory, list of keys, total, each item's percentage,
# most and least abundant (first-wins on tie), and a final dict after
# inserting one new item.

import sys


if __name__ == "__main__":
    pass
