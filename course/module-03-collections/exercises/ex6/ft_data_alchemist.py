# Exercise 6 — ft_data_alchemist
# Authorized: import random, random.*, print(), len(), sum(), round()
#
# Start with a fixed mixed-case list of player names.
# - list comprehension: all names capitalized.
# - list comprehension: only names that were already capitalized.
# - dict comprehension: {Name: random score} over the full capitalized list.
# - dict comprehension: scores above the average.

import random


if __name__ == "__main__":
    pass
