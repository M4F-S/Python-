# Exercise 1 — ft_score_analytics
# Authorized: import sys, sys.argv, len(), sum(), max(), min(), print()
#
# Parse scores from sys.argv. Discard non-numeric ones with an error.
# If nothing valid remains, print the usage line.
# Otherwise print: count, total, average, high, low, range.

import sys


if __name__ == "__main__":
    pass
