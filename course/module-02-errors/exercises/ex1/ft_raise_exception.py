# Exercise 1 — ft_raise_exception
# Authorized: int(), print()
#
# Extend ex0. After parsing the int, validate 0 <= temp <= 40.
# If too cold: raise ValueError(f"{temp}°C is too cold for plants (min 0°C)")
# If too hot:  raise ValueError(f"{temp}°C is too hot for plants (max 40°C)")
# Add test cases for "100" and "-50".


def input_temperature(temp_str: str) -> int:
    pass


def test_temperature() -> None:
    pass


if __name__ == "__main__":
    pass
