# Copy this file to `.env` and fill in real values.
# The real `.env` file must NOT be committed - it's in .gitignore.

MATRIX_MODE=development
DATABASE_URL=postgresql://user:password@localhost:5432/zion
API_KEY=replace-me-with-a-secret
LOG_LEVEL=DEBUG
ZION_ENDPOINT=https://zion.example.com
