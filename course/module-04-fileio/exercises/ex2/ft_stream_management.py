# Exercise 2 — ft_stream_management
# Authorized: import sys, sys.argv, sys.stdin, sys.stdout, sys.stderr,
#             len(), open(), import typing, typing.IO, io.read(),
#             io.readline(), io.write(), io.flush(), io.close(), print()
#
# Evolve ex1:
#   - print errors to stderr with "[STDERR] " prefix
#   - replace input() with sys.stdin.readline() (remember to strip \n)

import sys
from typing import IO


if __name__ == "__main__":
    pass
