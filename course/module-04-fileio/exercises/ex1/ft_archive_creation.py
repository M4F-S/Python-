# Exercise 1 — ft_archive_creation
# Authorized: import sys, sys.argv, len(), open(), import typing,
#             typing.IO, io.read(), io.write(), io.close(), print(), input()
#
# Same as ex0 plus:
#   - append '#' to each line to make "2087-compatible" archive
#   - display the transformed content
#   - ask for output filename; empty means don't save
#   - write to file if given

import sys
from typing import IO


if __name__ == "__main__":
    pass
